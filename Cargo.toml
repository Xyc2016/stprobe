[package]
name = "stprobe"
version = "0.1.0"
edition = "2021"
description = "A minimal CLI for inspecting safetensors headers"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
memmap2 = "0.9"
safetensors = "0.7"
